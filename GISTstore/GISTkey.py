# USE:
#import GISTkey
#username=GISTkey.gistkey()[0]
#password=GISTkey.gistkey()[1]
def gistkey():
    username='username'
    password="password"
    GistAuth = (username, password)
    return GistAuth